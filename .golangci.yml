version: "2"

run:
  timeout: 10m
  modules-download-mode: readonly
  tests: true

linters:
  default: none
  enable:
    - copyloopvar
    - depguard
    - errcheck
    - errorlint
    - forbidigo
    - govet
    - ineffassign
    - misspell
    - nolintlint
    - staticcheck
    - thelper
    - unconvert
    - unused
    - usestdlibvars
    - whitespace
  settings:
    depguard:
      rules:
        main:
          list-mode: lax
          deny:
            - pkg: github.com/pkg/errors
              desc: Use standard library errors/fmt wrapping instead.
            - pkg: io/ioutil
              desc: Use os or io packages instead of io/ioutil.
    errcheck:
      check-type-assertions: true
      exclude-functions:
        - (io.Closer).Close
        - os.Remove
        - os.RemoveAll
        - (*os.File).Close
        - (*compress/gzip.Reader).Close
        - (*github.com/tlepoid/tumux/internal/pty.Terminal).Close
    errorlint:
      asserts: false
      comparison: false
      errorf: true
    govet:
      enable:
        - nilness
        - reflectvaluecompare
        - sortslice
        - unusedwrite
    misspell:
      locale: US
    nolintlint:
      require-explanation: true
      require-specific: true
    forbidigo:
      exclude-godoc-examples: true
      forbid:
        - pattern: ^print(ln)?$
          msg: Avoid direct stdout/stderr prints in production code.
        - pattern: ^fmt\.Print.*$
          msg: Use internal/logging for internal packages; stdout/stderr output is reserved for CLI entrypoints.
        - pattern: ^log\.(Print|Printf|Println|Fatal|Fatalf|Fatalln|Panic|Panicf|Panicln)$
          msg: Use internal/logging and explicit error returns instead of stdlib log side effects.
    thelper:
      test:
        begin: false
      benchmark:
        begin: false
      tb:
        begin: false
      fuzz:
        begin: false
  exclusions:
    rules:
      - linters:
          - forbidigo
        path: ^cmd/
      - linters:
          - forbidigo
        path: ^internal/cli/
      - linters:
          - forbidigo
        path: _test\.go

formatters:
  enable:
    - gofmt
    - goimports
  settings:
    goimports:
      local-prefixes: github.com/tlepoid/tumux

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
